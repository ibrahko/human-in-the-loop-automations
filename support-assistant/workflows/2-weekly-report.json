{
  "id": "hitlSupReport001",
  "name": "Support — 2. Weekly report: are the drafts worth it?",
  "description": "Measures how often drafts are sent as is, edited or rejected, and how fast humans decide.",
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 9 * * 1"
            }
          ]
        }
      },
      "id": "cf7fcaf9-4218-5d59-b979-c3beb2f73a10",
      "name": "Every Monday at 09:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {},
      "id": "1ed4be93-f5f3-5319-81c3-674e3a6db9ba",
      "name": "Run now",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        200
      ]
    },
    {
      "parameters": {
        "mode": "raw",
        "jsonOutput": "{\n  \"telegram_chat_id\": \"PUT_YOUR_TELEGRAM_CHAT_ID_HERE\",\n  \"window_days\": 28,\n  \"min_decisions\": 20,\n  \"max_reject_rate\": 0.3\n}",
        "options": {}
      },
      "id": "8b11302a-d6bb-519f-8411-b637e832ab2a",
      "name": "Config",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        240,
        100
      ]
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "get",
        "dataTableId": {
          "__rl": true,
          "mode": "name",
          "value": "support_tickets"
        },
        "returnAll": true
      },
      "id": "649e9596-b66b-5be6-8191-05f0ef75c42e",
      "name": "Get all tickets",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        480,
        100
      ],
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// Are the AI drafts saving time, or creating work? Decide with numbers.\nconst cfg = $('Config').first().json;\nconst pct = (x) => `${Math.round(x * 100)}%`;\nconst all = $input.all().map((i) => i.json).filter((r) => r.ticket_id);\n// Judge the drafts on recent tickets only, so a recent drift is not hidden by old results.\nconst since = Date.now() - cfg.window_days * 24 * 60 * 60 * 1000;\nconst rows = all.filter((r) => new Date(r.createdAt || r.received_at).getTime() >= since);\n\nconst humanOnly = rows.filter((r) => r.route === 'human_only');\nconst drafted = rows.filter((r) => r.route === 'needs_approval');\nconst decided = drafted.filter((r) => ['sent_as_is', 'sent_edited', 'rejected'].includes(r.decision));\nconst count = (d) => decided.filter((r) => r.decision === d).length;\nconst asIs = count('sent_as_is');\nconst editedN = count('sent_edited');\nconst rejected = count('rejected');\nconst expired = drafted.filter((r) => r.decision === 'expired').length;\nconst waiting = drafted.filter((r) => !r.decision).length;\n\nconst minutes = decided.map((r) => Number(r.minutes_to_decision)).filter((m) => Number.isFinite(m)).sort((a, b) => a - b);\nconst mid = Math.floor(minutes.length / 2);\nconst median = !minutes.length ? null : minutes.length % 2 ? minutes[mid] : Math.round((minutes[mid - 1] + minutes[mid]) / 2);\n\nconst byCategory = {};\nfor (const r of rows) byCategory[r.category || 'unknown'] = (byCategory[r.category || 'unknown'] || 0) + 1;\n\nlet verdict;\nif (decided.length < cfg.min_decisions) {\n  verdict = `Too early to judge the drafts: ${decided.length} of ${cfg.min_decisions} decisions needed.`;\n} else if (rejected / decided.length > cfg.max_reject_rate) {\n  verdict = `PAUSE DRAFTING: ${pct(rejected / decided.length)} of drafts rejected (limit ${pct(cfg.max_reject_rate)}). ` +\n    'Set drafting_enabled to false in the intake workflow so every request goes to a person, then fix the prompt.';\n} else {\n  verdict = `OK: ${pct((asIs + editedN) / decided.length)} of drafts were used (${pct(asIs / decided.length)} unchanged).`;\n}\n\nconst lines = [\n  'Support: weekly report',\n  `Last ${cfg.window_days} days: ${rows.length} requests · handled by a person only: ${humanOnly.length}` +\n    (rows.length ? ` (${pct(humanOnly.length / rows.length)})` : '') + ` · drafts proposed: ${drafted.length}`,\n  `All time: ${all.length} requests`,\n  `Drafts decided: ${decided.length} (sent as is ${asIs}, edited ${editedN}, rejected ${rejected}) · expired: ${expired} · waiting: ${waiting}`,\n  `Median time to a human decision: ${median === null ? 'n/a' : `${median} min`}`,\n  `By category: ${Object.entries(byCategory).map(([k, v]) => `${k} ${v}`).join(', ') || 'none'}`,\n  '',\n  verdict,\n];\nreturn [{ json: { text: lines.join('\\n') } }];\n"
      },
      "id": "f72b1ea7-ca66-5fe5-89ef-08a088912372",
      "name": "Compute the metrics",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        720,
        100
      ]
    },
    {
      "parameters": {
        "chatId": "={{ $('Config').first().json.telegram_chat_id }}",
        "text": "={{ $json.text }}",
        "additionalFields": {
          "appendAttribution": false,
          "disable_web_page_preview": true,
          "parse_mode": "HTML"
        }
      },
      "id": "ff698569-0cd0-53cf-8d32-366db45b77a6",
      "name": "Send the report on Telegram",
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [
        960,
        100
      ]
    },
    {
      "parameters": {
        "content": "## When to stop drafting\nIf humans reject more than `max_reject_rate` of the drafts (after at least `min_decisions`), the report says to pause drafting: set `drafting_enabled` to `false` in the intake workflow so every request goes to a human, then fix the prompt. Only the last `window_days` count.",
        "height": 260,
        "width": 520
      },
      "id": "7177669f-aa65-5585-a4d0-a1c61f189dce",
      "name": "Kill switch",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -60,
        -320
      ]
    }
  ],
  "connections": {
    "Every Monday at 09:00": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run now": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Get all tickets",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get all tickets": {
      "main": [
        [
          {
            "node": "Compute the metrics",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Compute the metrics": {
      "main": [
        [
          {
            "node": "Send the report on Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "meta": {
    "templateCredsSetupCompleted": false
  }
}
